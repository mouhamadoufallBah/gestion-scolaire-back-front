import { ClasseIdToClasseInfoPipe } from './classe-id-to-classe-info.pipe';

describe('ClasseIdToClasseInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new ClasseIdToClasseInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
